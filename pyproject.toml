[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ronspot-sniper"
version = "0.2.1"
description = "Caza plazas de parking en Ronspot en cuanto alguien libera hueco"
readme = "README.md"
license = "MIT"
keywords = ["ronspot", "parking", "automation", "cron"]
requires-python = ">=3.11"
dependencies = ["requests>=2.31"]

[dependency-groups]
dev = ["pytest>=8", "ruff>=0.6", "mypy>=1.11", "types-requests"]

[project.scripts]
ronspot-sniper = "ronspot_sniper.cli:main"

[tool.setuptools]
packages = [
    "ronspot_sniper",
    "ronspot_sniper.adapters",
    "ronspot_sniper.adapters.notify",
    "ronspot_sniper.adapters.ronspot",
    "ronspot_sniper.adapters.state",
    "ronspot_sniper.application",
    "ronspot_sniper.cli",
    "ronspot_sniper.domain",
    "ronspot_sniper.ports",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "C4", "RET", "ARG", "PTH", "RUF"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["ARG"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unreachable = true
files = ["ronspot_sniper", "tests"]

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false
